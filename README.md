# Chop Chop Inc. Wiki

Static, bilingual game wiki built with Next.js 15, TypeScript and Tailwind CSS.

```bash
npm install
npm run validate
npm run build
```

The static Cloudflare Pages artifact is written to `out/`. Use `npm run dev` for local development.
