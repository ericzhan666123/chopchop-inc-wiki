import {ItemPage} from "@/components/pages";import {bySlug,items,slugifyId,text} from "@/lib/content";import {pageMetadata} from "@/lib/metadata";export const dynamicParams=false;export function generateStaticParams(){return items.map(i=>({slug:slugifyId(i.id)}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const i=bySlug(items,slug);return pageMetadata(i?text(i.name,"en"):"Item",i?.description?text(i.description,"en"):"Item details and recipes.","en",`/items/${slug}`)}export default async function Page({params}:{params:Promise<{slug:string}>}){return <ItemPage slug={(await params).slug} locale="en"/>}
