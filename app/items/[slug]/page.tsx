import {ItemPage} from "@/components/pages";import {bySlug,items,slugifyId} from "@/lib/content";import{itemSeo,seoPageMetadata}from"@/lib/seo-copy";export const dynamicParams=false;export function generateStaticParams(){return items.map(i=>({slug:slugifyId(i.id)}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const{slug}=await params,i=bySlug(items,slug),seo=i?itemSeo(i,"en"):{title:"Item",description:"Item details and recipes."};return seoPageMetadata(seo.title,seo.description,"en",`/items/${slug}`)}export default async function Page({params}:{params:Promise<{slug:string}>}){return <ItemPage slug={(await params).slug} locale="en"/>}
