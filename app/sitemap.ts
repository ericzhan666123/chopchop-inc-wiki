import type {MetadataRoute} from "next";import {itemCategories,items,pathFor,recipeCategories,slugifyId,visibleRecipes} from "@/lib/content";
import {achievementData} from "@/lib/steam";
export const dynamic="force-static";
const origin=process.env.NEXT_PUBLIC_SITE_URL??"https://chop-chop-inc-wiki.pages.dev";
export default function sitemap():MetadataRoute.Sitemap{const common=["","/database","/world","/game-info","/recipes","/items","/ducks","/trees","/audiences","/npcs","/achievements","/updates","/system-requirements","/about","/terms","/privacy"];const paths=[...common,...achievementData.achievements.map(a=>`/achievements/${a.slug}`),...recipeCategories.map(category=>`/recipes/category/${category}`),...itemCategories.map(category=>`/items/category/${category}`),...visibleRecipes.map(r=>`/recipes/${slugifyId(r.id)}`),...items.map(i=>`/items/${slugifyId(i.id)}`)];return paths.flatMap(path=>(["en","de"] as const).map(locale=>({url:`${origin}${pathFor(locale,path)}`,changeFrequency:"weekly" as const,priority:path===""?1:path.split("/").length>2?.7:.8,alternates:{languages:{en:`${origin}${pathFor("en",path)}`,de:`${origin}${pathFor("de",path)}`}}})))}
