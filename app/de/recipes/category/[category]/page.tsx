import {CategoryPage} from "@/components/pages";import {categoryLabel,recipeCategories} from "@/lib/content";import {pageMetadata} from "@/lib/metadata";export const dynamicParams=false;export function generateStaticParams(){return recipeCategories.map(category=>({category}))}export async function generateMetadata({params}:{params:Promise<{category:string}>}){const{category}=await params;const name=categoryLabel(category,"de");return pageMetadata(`${name} Rezepte - Chop Chop Inc. Wiki`,`${name}-Rezepte aus den Spieldaten von Chop Chop Inc.`,"de",`/recipes/category/${category}`)}export default async function Page({params}:{params:Promise<{category:string}>}){return <CategoryPage kind="recipes" category={(await params).category} locale="de"/>}
