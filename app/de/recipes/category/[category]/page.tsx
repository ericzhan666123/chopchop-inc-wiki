import {CategoryPage} from "@/components/pages";import {categoryLabel,recipeCategories} from "@/lib/content";import{categorySeo,seoPageMetadata}from"@/lib/seo-copy";export const dynamicParams=false;export function generateStaticParams(){return recipeCategories.map(category=>({category}))}export async function generateMetadata({params}:{params:Promise<{category:string}>}){const{category}=await params,seo=categorySeo("recipes",category,categoryLabel(category,"de"),"de");return seoPageMetadata(seo.title,seo.description,"de",`/recipes/category/${category}`)}export default async function Page({params}:{params:Promise<{category:string}>}){return <CategoryPage kind="recipes" category={(await params).category} locale="de"/>}
