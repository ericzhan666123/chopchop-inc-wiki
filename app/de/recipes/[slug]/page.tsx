import {RecipePage} from "@/components/pages";import {bySlug,slugifyId,visibleRecipes} from "@/lib/content";import{recipeSeo,seoPageMetadata}from"@/lib/seo-copy";export const dynamicParams=false;export function generateStaticParams(){return visibleRecipes.map(r=>({slug:slugifyId(r.id)}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const{slug}=await params,r=bySlug(visibleRecipes,slug),seo=r?recipeSeo(r,"de"):{title:"Rezept",description:"Rezeptdetails, Zutaten und Ergebnisse."};return seoPageMetadata(seo.title,seo.description,"de",`/recipes/${slug}`)}export default async function Page({params}:{params:Promise<{slug:string}>}){return <RecipePage slug={(await params).slug} locale="de"/>}
