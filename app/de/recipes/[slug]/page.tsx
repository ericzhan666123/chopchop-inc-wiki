import {RecipePage} from "@/components/pages";import {bySlug,recipePageTitle,slugifyId,visibleRecipes} from "@/lib/content";import {pageMetadata} from "@/lib/metadata";export const dynamicParams=false;export function generateStaticParams(){return visibleRecipes.map(r=>({slug:slugifyId(r.id)}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const r=bySlug(visibleRecipes,slug);return pageMetadata(r?recipePageTitle(r,"de")??"Rezept":"Rezept","Rezeptdetails, Zutaten und Ergebnisse.","de",`/recipes/${slug}`)}export default async function Page({params}:{params:Promise<{slug:string}>}){return <RecipePage slug={(await params).slug} locale="de"/>}
