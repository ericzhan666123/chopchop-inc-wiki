import{AchievementPage}from"@/components/InfoPages";import{achievementData}from"@/lib/steam";import{pageMetadata}from"@/lib/metadata";export const dynamicParams=false;export function generateStaticParams(){return achievementData.achievements.map(({slug})=>({slug}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const{slug}=await params,a=achievementData.achievements.find(x=>x.slug===slug);return pageMetadata(`${a?.displayName??"Errungenschaft"} - Chop Chop Inc.`,a?.description??"Verborgene Errungenschaft", "de",`/achievements/${slug}`)}export default async function Page({params}:{params:Promise<{slug:string}>}){return <AchievementPage locale="de" slug={(await params).slug}/>}
