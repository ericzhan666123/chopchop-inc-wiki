import {CategoryPage} from "@/components/pages";import {categoryLabel,itemCategories} from "@/lib/content";import {pageMetadata} from "@/lib/metadata";export const dynamicParams=false;export function generateStaticParams(){return itemCategories.map(category=>({category}))}export async function generateMetadata({params}:{params:Promise<{category:string}>}){const{category}=await params;const name=categoryLabel(category,"de");return pageMetadata(`${name} - Chop Chop Inc. Wiki`,`${name} aus den Spieldaten von Chop Chop Inc.`,"de",`/items/category/${category}`)}export default async function Page({params}:{params:Promise<{category:string}>}){return <CategoryPage kind="items" category={(await params).category} locale="de"/>}
