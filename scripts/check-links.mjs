import fs from "node:fs";import path from "node:path";import process from "node:process";
function files(dir){return fs.readdirSync(dir,{withFileTypes:true}).flatMap(e=>e.isDirectory()?files(path.join(dir,e.name)):[path.join(dir,e.name)])}const urls=new Set();for(const file of files(path.resolve("content"))){const text=fs.readFileSync(file,"utf8");for(const match of text.matchAll(/https?:\/\/[^\s"'<>\\)\]]+/g))urls.add(match[0])}const failures=[];for(const url of urls){try{const res=await fetch(url,{redirect:"follow",signal:AbortSignal.timeout(15000)});if(!res.ok)failures.push(`${res.status} ${url}`)}catch(error){failures.push(`${error.message} ${url}`)}}if(failures.length){console.error(failures.join("\n"));process.exit(1)}console.log(`External links valid: ${urls.size} URL(s) checked.`);
